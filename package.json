{
  "name": "parisrp",
  "version": "1.0.0",
  "description": "Fiche personnage GTA RP — Jeremy De Rocherfort",
  "scripts": {
    "sass:watch": "sass --watch scss/main.scss css/style.css --style expanded",
    "sass:build": "sass scss/main.scss css/style.css --style compressed --no-source-map"
  },
  "devDependencies": {
    "sass": "^1.77.0"
  }
}